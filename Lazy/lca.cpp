#include <bits/stdc++.h>
using namespace std;

class LCA {
public:
    int n;
    vector<vector<int>> adj;
    vector<vector<int>> 
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    
    
    return 0;
}