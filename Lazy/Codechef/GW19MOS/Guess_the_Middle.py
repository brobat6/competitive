def solve(mask, max, digit):
    # The number of numbers in the range [0, max] that have digits in mask set and rest arbitrary
    n = len(str(max))
    

def find_greatest(left, right, digit):
    # Find all favourite numbers in range
    pass

for _ in range(int(input())):
    a, b, c, d = map(int, input().split())