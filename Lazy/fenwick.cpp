#include <bits/stdc++.h>
using namespace std;
#define int long long

template <typename T>
class Fenwick {
    vector <T> fen;
    int n;

    Fenwick(int n) {
        this.n = n;
        fen.resize(n);
    }
};

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    
    
    return 0;
}