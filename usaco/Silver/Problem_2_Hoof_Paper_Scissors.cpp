// http://www.usaco.org/index.php?page=viewproblem2&cpid=691

#include <bits/stdc++.h>
#define forn(i, n) for (int i = ; i <= int(n); i++)
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cout.precision(numeric_limits<double>::max_digits10);
    // freopen("hps.in","r",stdin);
    // freopen("hps.out","w",stdout);
    // 6 arrays. xl,yl,zl,xr,yr,zr.
    int n;
    cin >> n;
    int xl[n+1],yl[n+1],zl[n+1],xr[n+1],yr[n+1],zr[n+1];
    char temp;
    forn(i, n) {
        cin >> temp;
        if(temp)
    }
    return 0;
}
